<!DOCTYPE html>
<html>
  <head>
    <title>IPA Reviewer</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <% if logged_in? %>
    <tr><%= button_to "Log out", logout_path, method: :delete %>
    <%= button_to "My Profile", user_path(current_user), method: :get %>
    <%= button_to "All IPAs", ipas_path, method: :get%>
    <%= button_to "Home Page", root_path, method: :get%></tr>
  <% else %>
    <%= link_to "Sign Up", new_user_path %>
    <%= link_to "Sign In", signin_path %>
  <% end %>
  
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <center><%= yield %></center>
  </body>
  <br>
  </br>
  <footer>
 
  </footer>
</html>
